"""Synthetic, deterministic fixtures standing in for the pipeline's external inputs.

See `make_fixtures.py` for what each fixture contains and why - the short version is
`tests/test_clean_clone.py` needs something shaped like the 640 GB data tree without
containing any of it.
"""
