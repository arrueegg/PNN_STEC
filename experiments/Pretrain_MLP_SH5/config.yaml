data:
  GNSS_data_path: /home/space/data/IONO/STEC_DB_CASDCB
  SH_degree: 5
  SWI_data_path: /home/space/data/IONO/SWI/
  every_x_doy: 40
  max_elevation: 90.0
  min_elevation: 5.0
  move_to_scratch: true
  sampling: ultra
  scratch_dir: /scratch2/arrueegg/WP4/PNN_STEC/data/
  shuffle: true
  train_subset_size: 10000000
  use_SWI: true
  use_agg_h5: true
debug: true
doy: '001'
finetune:
  epochs: 5
  learning_rate: 0.001
  num_workers: 8
  scheduler: StepLR
  scheduler_step_size: 10
mode: pretrain
model:
  input_size: 34
  model_type: MLP
  output_size: 1
output_dir: experiments/Pretrain_MLP_SH5
pretrain:
  batchsize: 512
  early_stopping: true
  epochs: 1
  learning_rate: 0.001
  num_workers: 8
  patience: 5
  save_model_every_epoch: false
  scheduler: StepLR
  scheduler_step_size: 10
pretrain_folder: ''
project_name: PNN_STEC
random_seed: 42
split:
  test_ratio: 0.1
  train_ratio: 0.8
  val_ratio: 0.1
training:
  loss_function: MSELoss
  loss_weight: 0.0
  optimizer: Adam
  weight_decay: 0.0
year: '2023'
